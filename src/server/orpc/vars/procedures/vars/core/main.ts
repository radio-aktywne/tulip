import { events } from "./vars/events";
import { schedule } from "./vars/schedule";
import { shows } from "./vars/shows";

export const core = {
  events: events,
  schedule: schedule,
  shows: shows,
};
